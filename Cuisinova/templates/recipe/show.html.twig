{% extends 'base.html.twig' %}

{% block title %}{{ recipe.title }}{% endblock %}

{% block body %}
    <h1 class="text-align">{{ recipe.title }}</h1>

    <p>
        {{ recipe.content }}
    </p>
{% endblock %}
